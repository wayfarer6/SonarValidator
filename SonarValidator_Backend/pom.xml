<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.1.1</version>
        <relativePath /> <!-- lookup parent from repository -->
    </parent>
    <groupId>org.example</groupId>
    <artifactId>SonarValidator_Backend</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>SonarValidator_Backend</name>
    <description>SonarValidator_Backend</description>
    <url />
    <licenses>
        <license />
    </licenses>
    <developers>
        <developer />
    </developers>
    <scm>
        <connection />
        <developerConnection />
        <tag />
        <url />
    </scm>
    <properties>
        <java.version>26</java.version>
    </properties>
    <dependencies>

        <dependency>
            <groupId>org.antlr</groupId>
            <artifactId>antlr4-runtime</artifactId>
            <version>4.13.1</version>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webflux</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webmvc</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-websocket</artifactId>
        </dependency>
        <!--
            Spring Security: 아이디/비밀번호 기반 로그인.

            프론트엔드의 SignInForm 이 지금까지 쿠키에 이메일만 넣고 통과시키는
            자리표시자였습니다. 이 의존성을 넣으면 실제 인증/인가를 쓸 수 있습니다.

            세션 기반으로 갑니다(폼 로그인 + HttpSession). 이유:
              - 기존 화면이 쿠키 기반 흐름을 이미 전제로 하고 있습니다.
              - 같은 출처가 아니어도 CORS + credentials 로 세션 쿠키가 오갑니다.
            JWT 는 무상태 이점이 있지만, 지금 규모에서는 토큰 저장/갱신/폐기
            로직이 오히려 위험을 늘립니다.

            인증이 필요한 경로는 /api/v1/** 이고, Agent WebSocket
            (/api/v1/management, /api/v1/telemetry)은 C++ Prober 가 세션을
            가질 수 없으므로 예외로 둡니다. (Config/SecurityConfig 참고)
        -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>
        <!--
            Spring Security 테스트 지원 (spring-security-test).

            @WithMockUser 등으로 인증을 가정한 컨트롤러 테스트를 쓰기 위함입니다.
        -->
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <scope>test</scope>
        </dependency>
        <!--
            Spring Data JPA: 엔티티(@Entity) 기반 영속성 계층.
            Hibernate 가 기본 JPA 구현체로 함께 들어옵니다.
        -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <!--
            PostgreSQL JDBC 드라이버 (운영 DB).

            JPA 스타터만 추가하고 드라이버가 없으면 기동 시
            "Failed to determine a suitable driver class" 로 실패합니다.
            버전은 spring-boot-dependencies 가 관리하므로 생략합니다.
        -->
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>
        <!--
            H2: 외부 DB 서버 없이 띄우는 개발/테스트용 임베디드 DB.
            `local` 프로필(application-local.yml)과 테스트에서만 사용합니다.
        -->
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>runtime</scope>
        </dependency>
        <!--
            H2 웹 콘솔 (/h2-console).

            Spring Boot 4 에서는 H2 콘솔 자동구성이 spring-boot-autoconfigure 에서
            분리되어 이 모듈로 옮겨졌습니다. 이 의존성이 없으면
            spring.h2.console.enabled=true 로 두어도 콘솔이 404 를 반환합니다.
            개발 편의용이므로 런타임 스코프로 둡니다.
        -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-h2console</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-devtools</artifactId>
            <scope>runtime</scope>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webmvc-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-websocket-test</artifactId>
            <scope>test</scope>
        </dependency>
        <!--
            JPA 테스트 지원 (@DataJpaTest 등).

            Spring Boot 4 에서는 테스트 슬라이스 애노테이션이 본체에서 분리되어
            모듈별 *-test 아티팩트로 제공됩니다. 이 의존성이 없으면
            org.springframework.boot.test.autoconfigure.orm.jpa 패키지를 찾지 못해
            컴파일이 실패합니다.
        -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa-test</artifactId>
            <scope>test</scope>
        </dependency>

    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.antlr</groupId>
                <artifactId>antlr4-maven-plugin</artifactId>
                <version>4.13.1</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>antlr4</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <!--
                      Visitor 패턴만 사용합니다. 순회는 ANTLR 이 자동 생성한
                      <Name>BaseVisitor 를 그대로 쓰고, Listener 는 쓰지 않으므로
                      생성을 끄어 불필요한 클래스를 만들지 않습니다.
                    -->
                    <visitor>true</visitor>
                    <listener>false</listener>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <executions>
                    <execution>
                        <id>default-compile</id>
                        <phase>compile</phase>
                        <goals>
                            <goal>compile</goal>
                        </goals>
                        <configuration>
                            <annotationProcessorPaths>
                                <path>
                                    <groupId>org.projectlombok</groupId>
                                    <artifactId>lombok</artifactId>
                                </path>
                            </annotationProcessorPaths>
                        </configuration>
                    </execution>
                    <execution>
                        <id>default-testCompile</id>
                        <phase>test-compile</phase>
                        <goals>
                            <goal>testCompile</goal>
                        </goals>
                        <configuration>
                            <annotationProcessorPaths>
                                <path>
                                    <groupId>org.projectlombok</groupId>
                                    <artifactId>lombok</artifactId>
                                </path>
                            </annotationProcessorPaths>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>